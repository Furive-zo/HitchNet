experiment:
  name: E1_charger_imu_gru
  type: in_domain
  seed: 42

model_config: models/imu_gru.yaml
dataset_config: datasets/charger.yaml

train:
  epochs: 50
  batch_size: 32
  lr: 0.0001
  weight_decay: 0.00001
  scheduler: cosine

eval:
  apply_kf: false
  save_predictions: true
