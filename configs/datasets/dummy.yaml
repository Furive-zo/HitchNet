dataset:
  name: "dummy"
  root: "datasets/LI-HAE/dataset/dummy_trailer"
  split: "datasets/LI-HAE/splits/dummy_trailer_split.json"
  num_workers: 16

  # -------------------------------------------
  # LiDAR branch parameters
  # -------------------------------------------
  pcd_max_points: 1000

  # -------------------------------------------
  # Temporal branch parameters
  # -------------------------------------------
  temporal_window: 20        # = 20 frames = 2 seconds
  micro_seq_length: 10       # = sensor sampling
